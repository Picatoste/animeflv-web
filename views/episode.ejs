<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= episode.title %> - Episodio <%= episode.number %></title>
</head>
<body>
  <h1><%= episode.title %> - Episodio <%= episode.number %></h1>

  <h2>Servidores disponibles</h2>
  <ul>
    <% episode.servers.forEach(server => { %>
      <li>
        <a href="<%= server.embed %>" target="_blank" rel="noopener noreferrer">
          <%= server.name %>
        </a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
